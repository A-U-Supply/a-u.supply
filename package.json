{
  "name": "au-supply",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "dev:api": "uvicorn main:app --reload --port 5000",
    "build": "astro build",
    "preview": "astro preview",
    "format": "prettier --write 'src/**/*.{astro,js,ts,css,svelte}'",
    "format:check": "prettier --check 'src/**/*.{astro,js,ts,css,svelte}'"
  },
  "devDependencies": {
    "prettier": "^3.5.0",
    "prettier-plugin-astro": "^0.14.0",
    "prettier-plugin-svelte": "^3.5.0"
  },
  "dependencies": {
    "@astrojs/svelte": "^7.2.5",
    "astro": "^5.7.10",
    "sortablejs": "^1.15.7",
    "svelte": "^5.55.3"
  }
}
